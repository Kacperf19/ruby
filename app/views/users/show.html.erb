<p style="color: green"><%= notice %></p>

<%= render @user %>

<div class="kac">
  <%= button_to 'Edytuj', edit_user_path(@user), method: :get,  class: 'btn btn-secondary'%>
  <%= button_to 'Powrót',  users_path, method: :get, class: 'btn btn-secondary' %>

</div>
